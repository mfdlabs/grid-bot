name: Deploy

on:
  workflow_dispatch:
    inputs:
      components:
        description: "The list of components to deploy"
        required: true
        type: string

      nomad_environment:
        description: "The environment to deploy to"
        required: true
        default: "production"
        type: choice
        options:
          - staging
          - production

      # In the form of "component,cpu:ram;component2,cpu:ram"
      nomad_resources:
        description: "The resources to use when deploying"
        required: false
        default: "grid-bot,1000:2048"
        type: string

      dry_run:
        description: "Whether to perform a dry run (i.e., validate the deployment without actually deploying)"
        required: false
        default: false
        type: boolean

permissions:
  deployments: write
  pull-requests: write

jobs:
  parse-input-components:
    name: Validate and find component directories
    runs-on: ubuntu-latest

    outputs:
      per-component-deployment-scripts: ${{ steps.read-deployment-scripts.outputs.per-component-deployment-scripts }}

      nomad-files: ${{ steps.components-to-nomad-jobs.outputs.nomad-files }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download Needed Node Modules
        run: npm install yaml

      - name: Validate and Find components
        uses: mfdlabs/component-finder-action@v10
        id: find-component-directories
        with:
          components: ${{ github.event.inputs.components }}
          component-search-directories: services

      - name: Read Deployment Scripts
        uses: actions/github-script@v7
        id: read-deployment-scripts
        env:
          NOMAD_ENVIRONMENT: ${{ github.event.inputs.nomad_environment }}
          NOMAD_SHORT_ENVIRONMENT: ${{ github.event.inputs.nomad_environment == 'production' && 'prod' || 'stage' }}
          RBX_ENVIRONMENT: ${{ github.event.inputs.nomad_environment == 'production' && 'sitetest1' || 'sitetest3' }}

          VAULT_ADDR: ${{ vars.VAULT_ADDR }}
          VAULT_TOKEN: ${{ vars.VAULT_TOKEN }}
        with:
          script: |
            const fs = require('fs');
            const path = require('path');
            const yaml = require('yaml');

            let components = ${{ steps.find-component-directories.outputs.components }};
            components = new Map(Object.entries(components));

            let perComponentDeploymentScripts = {};

            for (const [component, configFileName] of components) {
              const [, componentVersion] = component.split(':');

              process.env.NOMAD_VERSION = componentVersion;

              // Parse yaml
              const componentConfigData = fs.readFileSync(configFileName, 'utf8');
              const replacedContents = componentConfigData.replace(
                /\$\{{ env.([A-Za-z_]+) }}/g, // back slash needed here to escape from github action
                (_, envVar) => {
                  const value = process.env[envVar]

                  if (!value) {
                    return 'undefined'
                  }

                  return value
                },
              )

              const componentConfig = yaml.parse(replacedContents);

              if (!componentConfig.deployment) {
                continue
              }

              let beforeDeploymentScripts = componentConfig.deployment.before || [];
              let afterDeploymentScripts = componentConfig.deployment.after || [];

              // Can either be:
              // after:
              //   - path: path/to/script
              // or
              // after:
              //   - script: raw script contents
              // (where script will create a temp file.)

              function mapDeploymentScriptsCallback(script) {
                if (script.path) {
                  // If not absolute path, make it relative to the component config file
                  const scriptPath = path.isAbsolute(script.path)
                    ? script.path
                    : path.join(path.dirname(configFileName), script.path)

                  // Read the script file to ensure it exists
                  if (!fs.existsSync(scriptPath)) {
                    throw new Error(`Deployment script file not found: ${scriptPath}`);
                  }
                  
                  const id = script.id ?? crypto.randomBytes(8).toString('hex');
                  
                  return {id, scriptData: fs.readFileSync(scriptPath, 'utf8')};
                }

                if (script.script) {
                  return {id: script.id ?? crypto.randomBytes(8).toString('hex'), scriptData: script.script};
                }

                throw new Error(`Invalid deployment script in ${configFileName}: ${JSON.stringify(script)}`);
              }
              
              beforeDeploymentScripts = beforeDeploymentScripts.map(mapDeploymentScriptsCallback);
              afterDeploymentScripts = afterDeploymentScripts.map(mapDeploymentScriptsCallback);

              perComponentDeploymentScripts[component] = {
                before: beforeDeploymentScripts,
                after: afterDeploymentScripts,
              };
            }

            core.setOutput('per-component-deployment-scripts', JSON.stringify(perComponentDeploymentScripts));

      - name: Components to Nomad Jobs
        uses: mfdlabs/component-nomad-parser-action@v18
        id: components-to-nomad-jobs
        env:
          NOMAD_ENVIRONMENT: ${{ github.event.inputs.nomad_environment }}
          NOMAD_SHORT_ENVIRONMENT: ${{ github.event.inputs.nomad_environment == 'production' && 'prod' || 'stage' }}
          RBX_ENVIRONMENT: ${{ github.event.inputs.nomad_environment == 'production' && 'sitetest1' || 'sitetest3' }}

          VAULT_ADDR: ${{ vars.VAULT_ADDR }}
          VAULT_TOKEN: ${{ vars.VAULT_TOKEN }}
        with:
          components: ${{ steps.find-component-directories.outputs.components }}
          resources: ${{ github.event.inputs.nomad_resources }}

  find-related-pr:
    name: Find Related Pull Request
    runs-on: ubuntu-latest

    outputs:
      pr-number: ${{ steps.pr-number.outputs.pr-number }}

    steps:
      - name: Determine PR Number
        id: pr-number
        uses: actions/github-script@v7
        with:
          script: |
            console.log(`Event Name: ${{ github.event_name }}`);

            // Determine the PR number from the branch that triggered this workflow_dispatch
            const headRef = '${{ github.ref }}'.replace('refs/heads/', '');

            let prNumber = '';
            console.log(`Searching for PR with head: ${{ github.repository_owner }}:${headRef}`);

            const pr = await github.rest.pulls.list({
              owner: '${{ github.repository_owner }}',
              repo: '${{ github.repository }}'.split('/')[1],
              head: `${{ github.repository_owner }}:${headRef}`,
              state: 'open'
            });

            if (pr.data.length > 0) {
              prNumber = pr.data[0].number;
            }

            core.setOutput('pr-number', prNumber);

  deploy-nomad-jobs:
    name: Deploy Nomad Jobs
    runs-on: grid-bot-infra

    needs: parse-input-components
    environment: ${{ github.event.inputs.nomad_environment }}

    if: ${{ needs.parse-input-components.outputs.nomad-files != '{}' && needs.parse-input-components.outputs.nomad-files != '' }}

    outputs:
      failure-summary: ${{ steps.deploy-nomad-jobs.outputs.failure-summary }}

    env:
      NOMAD_ADDR: ${{ vars.NOMAD_ADDR }}
      NOMAD_TOKEN: ${{ secrets.NOMAD_TOKEN }}

    steps:
      - name: Setup Nomad CLI
        uses: nferch/setup-nomad@v4.0.0
        env:
          NOMAD_TLS_SKIP_VERIFY: 1

      - name: Deploy Nomad Jobs
        uses: actions/github-script@v7
        id: deploy-nomad-jobs
        env:
          NOMAD_ENVIRONMENT: ${{ github.event.inputs.nomad_environment }}
          NOMAD_SHORT_ENVIRONMENT: ${{ github.event.inputs.nomad_environment == 'production' && 'prod' || 'stage' }}
          RBX_ENVIRONMENT: ${{ github.event.inputs.nomad_environment == 'production' && 'sitetest1' || 'sitetest3' }}

          VAULT_ADDR: ${{ vars.VAULT_ADDR }}
          VAULT_TOKEN: ${{ vars.VAULT_TOKEN }}
        with:
          script: |
            const fs = require('fs');
            const path = require('path');
            const crypto = require('crypto');
            const child_process = require('child_process');

            const isDryRun = ${{ github.event.inputs.dry_run }};
            const perComponentDeploymentScripts = ${{ needs.parse-input-components.outputs.per-component-deployment-scripts }};
            let jobs = ${{ needs.parse-input-components.outputs.nomad-files }};
            jobs = new Map(Object.entries(jobs));

            const failedJobs = [];
            const failureSummary = [];

            const getJobNameFromDefinition = (jobDefinition, fallback) => {
              const match = jobDefinition.match(/^\s*job\s+"([^"]+)"/m);
              return match ? match[1] : fallback;
            };

            // Nomad jobs are not necessarily deployed to the "default" namespace
            // (e.g. component.yaml can set `namespace: grid-bot`), so we must
            // read it out of the generated HCL and pass it explicitly to every
            // nomad CLI invocation that targets an existing job by name.
            const getNamespaceFromDefinition = (jobDefinition) => {
              const match = jobDefinition.match(/^\s*namespace\s*=\s*"([^"]+)"/m);
              return match ? match[1] : 'default';
            };

            const getPreviousNomadVersion = (jobName, namespace) => {
              try {
                const history = child_process.execSync(`nomad job history -namespace=${namespace} -json ${jobName}`, {
                  encoding: 'utf8',
                  stdio: ['ignore', 'pipe', 'pipe'],
                });

                const parsed = JSON.parse(history);
                if (!Array.isArray(parsed) || parsed.length < 2) {
                  return null;
                }

                const previous = parsed[1];
                if (!previous || previous.Version == null) {
                  return null;
                }

                return String(previous.Version);
              } catch (error) {
                console.warn(`Unable to inspect previous Nomad history for ${jobName} (namespace: ${namespace}): ${error.message}`);
                return null;
              }
            };

            const getPreviousNomadJobDefinition = (jobName, namespace) => {
              try {
                return child_process.execSync(`nomad job inspect -namespace=${namespace} ${jobName}`, {
                  encoding: 'utf8',
                  stdio: ['ignore', 'pipe', 'pipe'],
                });
              } catch (error) {
                console.warn(`Unable to inspect previous Nomad job definition for ${jobName} (namespace: ${namespace}): ${error.message}`);
                return null;
              }
            };

            const rollbackNomadJob = (jobName, namespace, previousVersion, previousJobSpec) => {
              try {
                if (previousVersion) {
                  console.warn(`Attempting rollback for ${jobName} (namespace: ${namespace}) to Nomad version ${previousVersion}`);
                  child_process.execSync(`nomad job revert -namespace=${namespace} ${jobName} ${previousVersion}`, {
                    stdio: 'inherit',
                    env: process.env,
                  });
                  return true;
                }

                if (previousJobSpec) {
                  const restoreFile = path.join(
                    process.env.RUNNER_TEMP,
                    `nomad-rollback-${jobName.replace(/[^a-zA-Z0-9_.-]/g, '-')}.hcl`
                  );

                  fs.writeFileSync(restoreFile, previousJobSpec);

                  console.warn(`Attempting rollback for ${jobName} (namespace: ${namespace}) using the last-known-good definition.`);
                  child_process.execSync(`nomad job run -namespace=${namespace} ${restoreFile}`, {
                    stdio: 'inherit',
                    env: process.env,
                  });
                  return true;
                }

                console.warn(`No previous Nomad version or job definition was found for ${jobName} (namespace: ${namespace}); automatic rollback is not possible.`);
                return false;
              } catch (error) {
                console.error(`Rollback failed for ${jobName} (namespace: ${namespace})`);
                console.error(error);
                return false;
              }
            };

            for (const [jobName, jobDefinition] of jobs) {
              const [componentName] = jobName.split(':');
              const deploymentScripts = perComponentDeploymentScripts[jobName] || {};
              const nomadJobName = getJobNameFromDefinition(jobDefinition, componentName);
              const nomadNamespace = getNamespaceFromDefinition(jobDefinition);

              let previousVersion = null;
              let previousJobSpec = null;

              if (!isDryRun) {
                previousVersion = getPreviousNomadVersion(nomadJobName, nomadNamespace);
                previousJobSpec = getPreviousNomadJobDefinition(nomadJobName, nomadNamespace);
              }

              if (deploymentScripts.before) {
                for (const { id, scriptData } of deploymentScripts.before) {
                  console.log(`Running before deployment script for ${componentName}: ${id}`);

                  if (!isDryRun) {
                    try {
                      const scriptPath = path.join(process.env.RUNNER_TEMP, `before-deploy-${componentName}-${crypto.randomBytes(8).toString('hex')}.sh`);
                      fs.writeFileSync(scriptPath, scriptData, { mode: 0o755 });

                      child_process.execSync(scriptPath, { stdio: 'inherit', env: process.env });

                      fs.unlinkSync(scriptPath);
                    } catch (error) {
                      const reason = error?.stderr?.toString?.() || error?.message || String(error);

                      console.error(`Before deployment script failed for ${componentName}: ${scriptPath}`);
                      console.error(error);

                      failureSummary.push({ job: jobName, reason: `Before deployment script failed: ${reason}`.trim() });
                      failedJobs.push(jobName);

                      continue;
                    }
                  }
                }
              }

              const tempFile = path.join(process.env.RUNNER_TEMP, `nomad-job-${componentName}.hcl`);
              fs.writeFileSync(tempFile, jobDefinition);

              if (!isDryRun) {
                try {
                  child_process.execSync(`nomad job run -namespace=${nomadNamespace} ${tempFile}`, { stdio: 'inherit', env: process.env });
                } catch (error) {
                  const reason = error?.stderr?.toString?.() || error?.message || String(error);

                  console.error(`Deployment failed for ${jobName}; attempting rollback...`);

                  const rollbackSucceeded = rollbackNomadJob(nomadJobName, nomadNamespace, previousVersion, previousJobSpec);

                  failureSummary.push({ job: jobName, reason: reason.trim() });

                  if (!rollbackSucceeded) {
                    console.error(`Rollback was not possible for ${jobName}.`);
                    failureSummary.push({ job: jobName, reason: 'Automatic rollback failed or no rollback target was available.' });
                  } else {
                    failureSummary.push({ job: jobName, reason: 'Automatic rollback to the previous Nomad version/definition succeeded.' });
                  }

                  failedJobs.push(jobName);
                  continue;
                }
              } else {
                console.log(`Dry run: would have deployed job ${jobName} with definition:\n${jobDefinition}`);
              }

              if (deploymentScripts.after) {
                for (const { id, scriptData } of deploymentScripts.after) {
                  console.log(`Running after deployment script for ${componentName}: ${id}`);

                  if (!isDryRun) {
                    try {
                      const scriptPath = path.join(process.env.RUNNER_TEMP, `after-deploy-${componentName}-${crypto.randomBytes(8).toString('hex')}.sh`);
                      fs.writeFileSync(scriptPath, scriptData, { mode: 0o755 });

                      child_process.execSync(scriptPath, { stdio: 'inherit', env: process.env });

                      fs.unlinkSync(scriptPath);
                    } catch (error) {
                      console.warn(`After deployment script failed for ${componentName}: ${id}`);
                      console.warn(error);
                    }
                  }
                }
              }
            }

            core.setOutput('failure-summary', JSON.stringify(failureSummary));

            if (failedJobs.length > 0) {
              core.setFailed(`Failed to deploy the following jobs: ${failedJobs.join(', ')}`);
            }

      - name: Mark old deployments inactive
        if: ${{ success() && github.event.inputs.dry_run == 'false' }}
        uses: actions/github-script@v7
        env:
          NOMAD_ENVIRONMENT: ${{ github.event.inputs.nomad_environment }}
        with:
          script: |
            const environment = process.env.NOMAD_ENVIRONMENT;

            const deployments = await github.rest.repos.listDeployments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              environment,
              per_page: 100,
            });

            // Sort newest first; skip the most recent (the one just created this run)
            const sorted = deployments.data.sort(
              (a, b) => new Date(b.created_at) - new Date(a.created_at)
            );
            const toInactivate = sorted.slice(1);

            for (const deployment of toInactivate) {
              // Check current state first to avoid redundant status entries
              const statuses = await github.rest.repos.listDeploymentStatuses({
                owner: context.repo.owner,
                repo: context.repo.repo,
                deployment_id: deployment.id,
                per_page: 1,
              });

              const latestState = statuses.data[0]?.state;
              if (latestState === 'inactive') continue;

              console.log(`Marking deployment ${deployment.id} (created ${deployment.created_at}) as inactive`);

              await github.rest.repos.createDeploymentStatus({
                owner: context.repo.owner,
                repo: context.repo.repo,
                deployment_id: deployment.id,
                state: 'inactive',
              });
            }

  comment-deploy-result:
    name: Comment Deployment Result
    runs-on: ubuntu-latest

    needs:
      - parse-input-components
      - find-related-pr
      - deploy-nomad-jobs

    if: ${{ always() && needs.find-related-pr.outputs.pr-number != '' }}

    steps:
      - name: Comment Deployment Result
        uses: actions/github-script@v7
        env:
          PR_NUMBER: ${{ needs.find-related-pr.outputs.pr-number }}
          NOMAD_UI_URL: ${{ vars.NOMAD_UI_URL }}
          NOMAD_ENVIRONMENT: ${{ github.event.inputs.nomad_environment }}
          DEPLOY_RESULT: ${{ needs.deploy-nomad-jobs.result }}
          FAILURE_SUMMARY: ${{ needs.deploy-nomad-jobs.outputs.failure-summary }}
        with:
          github-token: ${{ secrets.DEPLOYER_TOKEN }}
          script: |
            const prNumber = Number(process.env.PR_NUMBER);
            const environment = process.env.NOMAD_ENVIRONMENT;
            const result = process.env.DEPLOY_RESULT;
            const nomadUiUrl = (process.env.NOMAD_UI_URL || '').replace(/\/$/, '');

            const jobs = ${{ needs.parse-input-components.outputs.nomad-files }};
            const jobLinks = Object.entries(jobs || {})
              .map(([component]) => {
                const jobName = component.split(':')[0];
                const url = nomadUiUrl
                  ? `${nomadUiUrl}/ui/jobs/${encodeURIComponent(jobName)}`
                  : null;

                return url
                  ? `- **${jobName}**: [Nomad job](${url})`
                  : `- **${jobName}**`;
              })
              .join('\n') || '- No Nomad jobs were generated for this run.';

            let body;

            if (result === 'success') {
              body = [
                `## ✅ Deployment succeeded`,
                ``,
                `Environment: \`${environment}\``,
                ``,
                `Nomad jobs:`,
                jobLinks,
              ].join('\n');
            } else {
              const failureSummary = JSON.parse(process.env.FAILURE_SUMMARY || '[]');

              const failureDetails = failureSummary.length > 0
                ? failureSummary.map(({ job, reason }) => `- **${job}**: \`${reason}\``).join('\n')
                : '- No detailed failure information was captured.';

              body = [
                `## ❌ Deployment failed`,
                ``,
                `Environment: \`${environment}\``,
                ``,
                `Failure details:`,
                failureDetails,
                ``,
                `Nomad jobs:`,
                jobLinks,
                ``,
                `[View workflow run](${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId})`,
              ].join('\n');
            }

            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: prNumber,
              body,
            });
